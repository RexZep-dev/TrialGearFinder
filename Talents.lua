-- Коды талантов по спекам: строка, которую игра принимает в окне талантов
-- кнопкой «Загрузить сборку». Новичку не нужен список названий — ему нужен
-- код, который вставляется целиком.
--
-- ОТКУДА БЕРУТСЯ. Только от живых игроков: в игре окно талантов → «Экспорт»,
-- либо строка `talents=` из экспорта аддона SimulationCraft. Из логов
-- Warcraft Logs их взять нельзя: там таланты записаны номерами узлов дерева
-- без имён, и собрать из них строку загрузки не получится (проверено
-- 13 сентября — Wowhead такие номера тоже не знает).
--
-- Поэтому таблица наполняется по мере того, как люди присылают свои коды,
-- ровно как наполнялась база предметов.

local addonName, ns = ...

ns.TalentCodes = {
    -- Воин, Неистовство. Код из экспорта пользователя (Кавочавоо), 12 сентября.
    [72] = {
        code = "CgEASWsDSHNyPDXnbxuIhH3ZdDAAAAAAgGDgBGYAAGzAAAmBAACAAAAAAAAAAAAAAM",
        note = "Сборка Кавочавоо: проверена в бою, 247 заходов в логах рейтинга",
    },
}
